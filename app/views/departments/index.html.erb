<div class="flex flex-wrap space-y-4 md:flex-nowrap md:space-y-0 md:space-x-10">
  <%= render 'shared/settings_nav' %>

  <div class="flex-grow min-w-0">
    <div class="flex flex-col gap-2 md:flex-row md:justify-between md:items-center mb-4">
      <h1 class="text-4xl font-bold">Departments</h1>

      <%= link_to new_settings_department_path,
                  class: 'btn btn-primary btn-md gap-2 w-full md:w-fit',
                  data: { turbo_frame: 'modal' } do %>
        <%= svg_icon('plus', class: 'w-6 h-6') %>
        <span>New Department</span>
      <% end %>
    </div>

    <div class="overflow-x-auto">
      <table class="table w-full table-lg rounded-b-none overflow-hidden">
        <thead class="bg-base-200">
          <tr class="text-neutral uppercase">
            <th>Name</th>
            <th>Company</th>
            <th>Users</th>
            <th>Templates</th>
            <th class="text-right" width="1px"></th>
          </tr>
        </thead>

        <tbody>
          <% @departments.each do |department| %>
            <tr>
              <td><%= department.name %></td>
              <td><%= department.company&.name || 'No company' %></td>
              <td><%= department.users.size %></td>
              <td><%= department.templates.size %></td>
              <td class="flex items-center space-x-2 justify-end">
                <%= link_to 'Edit',
                            edit_settings_department_path(department),
                            class: 'btn btn-outline btn-xs',
                            data: { turbo_frame: 'modal' } %>

                <%= button_to 'Delete',
                              settings_department_path(department),
                              method: :delete,
                              class: 'btn btn-outline btn-error btn-xs',
                              data: { turbo_confirm: 'Are you sure?' } %>
              </td>
            </tr>
          <% end %>

          <% if @departments.blank? %>
            <tr>
              <td colspan="5" class="text-center opacity-70">
                No departments yet.
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
