<div class="form-control">
  <%= f.label :role, class: 'label' %>

  <%= f.select :role,
               User::ROLES.map { |role| [t(role, default: role.titleize), role] },
               { include_blank: false },
               class: 'base-select',
               data: { user_role_select: true } %>
</div>